# Local Setup Guide

## Required Tools

Install the following tools:

- Git
- Docker Desktop
- kubectl
- Minikube
- Terraform
- Helm
- Istio CLI

## Verify Installation

```bash
git --version
docker --version
kubectl version --client
minikube version
terraform version
helm version
istioctl version